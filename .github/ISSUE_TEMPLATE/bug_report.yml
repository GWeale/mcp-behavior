name: Bug report
description: Report reproducible MCP Behavior behavior.
title: "[bug] "
labels: [bug]
body:
  - type: markdown
    attributes:
      value: Remove credentials and private tool results before posting evidence.
  - type: input
    id: version
    attributes:
      label: MCP Behavior revision
      placeholder: main at commit 0123456, or a release version
    validations:
      required: true
  - type: dropdown
    id: transport
    attributes:
      label: Transport
      options: [stdio, Streamable HTTP, not transport-related]
    validations:
      required: true
  - type: textarea
    id: reproduction
    attributes:
      label: Minimal reproduction
      description: Include a sanitized contract and exact command when possible.
    validations:
      required: true
  - type: textarea
    id: observed
    attributes:
      label: Observed behavior
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
    validations:
      required: true
