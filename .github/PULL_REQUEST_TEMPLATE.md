## Change

Describe the observable behavior this changes.

## Verification

- [ ] Tests cover the public behavior or the narrow adapter seam.
- [ ] `uv run ruff format --check src tests` passes.
- [ ] `uv run ruff check src tests` passes.
- [ ] `uv run mypy` passes.
- [ ] `uv run pytest` passes.
- [ ] Contracts, logs, and evidence contain no credentials or private data.
- [ ] User-facing YAML or CLI changes include documentation.
